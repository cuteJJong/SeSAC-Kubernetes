apiVersion: v1
kind: Service
metadata:
  name: my-first-service
spec:
  selector:
    app: my-first-pod
  ports:
  - name: http
    port: 80
    targetPort: 80  #내부포트
    nodePort: 30080 #외부포트
  type: NodePort
apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  selector:
    app: spring-boot-app
  ports:
    - protocol: TCP
      port: 8080  # 외부에서 접근할 포트
      targetPort: 8080  # 컨테이너 내부의 포트
  type: NodePort  # 외부 접근을 위해 NodePort 또는 LoadBalancer 설정
